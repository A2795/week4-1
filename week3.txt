#!/bin/bash
echo "my week three script"
echo "Lets adentify and check permissions"
echo "hello world" > /def/null
echo "Lets change the directory"
cd 
echo "lets change switch to dev directory"
cd /dev
echo "lets list some files"
ls -l
echo "lets give a try dev console list"
ls -l /dev/console
echo " locate print block device"
blkid
echo "lets locate block list"
lsblk
echo " 
